.program rp_pwm
.side_set 1

.wrap_target
pull_high:
    pull block         side 1
    mov x, osr         side 1
high_loop:
    jmp x-- high_loop  side 1
    pull block         side 0
    mov x, osr         side 0
low_loop:
    jmp x-- low_loop   side 0
    jmp pull_high      side 0
.wrap